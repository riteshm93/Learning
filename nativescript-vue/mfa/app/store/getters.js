export const table = state => {
  return state.table;
};

export const tables = state => {
  return state.tables;
};

export const isItemsSetupDone = state => {
  return state.isItemsSetupDone;
};

export const isPropertiesSetupDone = state => {
  return state.isPropertiesSetupDone;
};

export const itemList = state => {
  return state.items;
};

export const propertyList = state => {
  return state.properties;
};

export const dataList = state => {
  return state.data;
};
